Make a file called my_file.txt in your home directory.
$touch my_file.txt
Put some text in the file using the echo command.
$echo "prot1:RLGV\nprot2:VVYW\nDNA3:FPRL\nDNA4:RLQY" >> my_file.txt

You should see the file when you open a terminal a type 'ls' enter.
$ls

Try the following commands. After running each set of commands
write/type what you think is happening. Each line is a different command.

1) Directory contents

ls
ls -a
ls -l
ls -al #look carefully at this output. Do you understand it?

2) Files and directories

mkdir TMP
ls
cp my_file.txt my_file_copy.txt
ls
mv my_file_copy.txt TMP/
cd TMP
ls

3) Moving around - use the 'pwd' command to see where you are: 

cd
cd TMP
cd .
cd ..
cd TMP
cd ~
cd /
ls
cd

For the next step, download the file 01C_data_file.txt to your home directory.
(If you cannot figure out how to do this, continue using my_file.txt in place of 01C_data_file.txt)

4) Fun with files! Use 'ls -al' to see the files.

cp 01C_data_file.txt tmp.txt
cat tmp.txt
cat 01C_data_file.txt tmp.txt > bigfile.txt
ls -al   #Compare the size fo all three files. 01C_data_file.txt, tmp.txt and bigfile.txt
mv bigfile.txt bigfile
gzip bigfile
ls -al #What happened to the size of bigfile?
gunzip bigfile.gz
cp bigfile TMP/ 

5) Looking at files

head bigfile
tail bigfile
less bigfile # Use spacebar and b, q to quit

6) Finding crap in files - what is happening?

grep 'RL' bigfile
grep 'RL' -c bigfile

grep 'RL' my_file.txt
grep 'RL' -c my_file.txt

grep -r 'RL' -c TMP/

7) Getting rid of files or directories

rm bigfile
rm my_file.txt tmp.txt
rm TMP  #did this work?
rm -r TMP





